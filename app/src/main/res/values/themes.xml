<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Window-background-only theme; all UI is rendered by Compose for TV. -->
    <style name="Theme.LOTR" parent="android:Theme.Black.NoTitleBar.Fullscreen">
        <item name="android:windowBackground">@color/lotr_background</item>
    </style>
</resources>
